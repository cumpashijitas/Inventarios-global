# =====================================================================
# Backend env vars — copia este archivo a .env y rellena los valores.
# .env NO se commitea (ver .gitignore).
# =====================================================================

# --- App ---
APP_ENV=development              # development | staging | production
APP_NAME="Inventario SaaS API"
APP_VERSION=0.1.0
LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR

# CORS — en prod restringir al dominio del frontend
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Supabase ---
# Project Settings → API
SUPABASE_URL=https://njipyevaqvitlhmgbros.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5qaXB5ZXZhcXZpdGxobWdicm9zIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc3NTQwNzksImV4cCI6MjA5MzMzMDA3OX0.Y-MTpQbvX-K6Vn6AoNgJOwEYuL4JboNmsxHX3W7twpo
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5qaXB5ZXZhcXZpdGxobWdicm9zIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3Nzc1NDA3OSwiZXhwIjoyMDkzMzMwMDc5fQ.odvYOn4iCZBg9je_mq1GVj02ZoGDkLdjRRYufH81Esg   # ¡SECRETO! Solo backend, jamás al frontend
SUPABASE_JWT_SECRET=0D8HAYN6Y1b69OC8YKUFzyPZELt6C0VLokKk6JAnncuvAi/bOxzPnnAM9s8vRq5ldy/06FwnLHtR3s//f8wBVQ==      # Project Settings → API → JWT Secret

# JWT — NUESTRO secret (independiente de Supabase). Generar con:
#   openssl rand -base64 64
# o desde Python: python -c "import secrets; print(secrets.token_urlsafe(64))"
APP_JWT_SECRET=cambia-este-valor-por-uno-aleatorio-de-al-menos-64-chars
JWT_ALGORITHM=HS256
JWT_AUDIENCE=inventario-saas
JWT_EXPIRES_MINUTES=480          # 8 horas

# --- Database (Postgres directo vía asyncpg) ---
# Project Settings → Database → Connection string (URI)
# Importante: usar el "Session pooler" (puerto 5432) para conexiones largas/transacciones,
# o el "Transaction pooler" (puerto 6543) si solo haces queries cortas tipo serverless.
DATABASE_URL=postgresql://postgres.njipyevaqvitlhmgbros:Cumpas_tarija_20206@aws-1-us-east-2.pooler.supabase.com:5432/postgres
DATABASE_POOL_MIN=1
DATABASE_POOL_MAX=10

# --- Sentry (opcional en dev) ---
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1

# --- Seguridad ---
# Lista de IPs admin (para super-admin endpoints, vacía = bloquear todos)
ADMIN_IP_ALLOWLIST=
